services:
  app-service:
    build:
      context: . # workspace root so path deps (test-helpers) resolve
      dockerfile: app-service/Dockerfile
  auth-service:
    build:
      context: . # workspace root so path deps (test-helpers) resolve
      dockerfile: auth-service/Dockerfile